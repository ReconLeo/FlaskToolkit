# FlaskToolkit 运行依赖（已验证版本组合，版本锁定以保证可复现）
# 安装：pip install -r requirements.txt

Flask==3.1.3
Flask-Cors==6.0.5
# APScheduler 4.x 为破坏性大版本（BackgroundScheduler 等 API 变更），锁定 3.x
APScheduler==3.11.3
watchdog==6.0.0
